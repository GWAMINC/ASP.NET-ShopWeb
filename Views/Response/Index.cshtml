@model IEnumerable<ShopWeb.Models.Domain.Response>

<div id="response-container">

    <h1>Phản hồi</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Người dùng</th>
                <th>Tiêu đề</th>
                <th>Nội dung</th>
                <th>Ngày tạo</th>
                <th>Trạng thái</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var response in Model)
            {
                <tr>
                    <td>@response.UserId</td>
                    <td>@response.Heading</td>
                    <td>@response.Content</td>
                    <td>@response.CreatedAt</td>
                    <td>@response.State</td>
                    <td>
                        <a asp-action="Details" asp-route-id="@response.Id">Details</a>
                    </td>
                    @if (response.State == "Pending")
                    {
                        <td>
                            <a class="btn btn-dark" asp-area="" asp-controller="Response" asp-action="Reply" asp-route-id="@response.Id">Reply</a>
                        </td>
                    }
                </tr>
            }
        </tbody>
    </table>
</div>
